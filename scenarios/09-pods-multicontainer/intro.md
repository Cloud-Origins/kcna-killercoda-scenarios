## Pods & Multi-container Patterns

Nothing pre-staged this time -- you're building both Pods from scratch. Kept short by writing exactly two Pods, not a whole app.

**You will:**
1. Create a plain single-container Pod
2. Create a two-container sidecar Pod where both containers share a volume

**Target time:** 50 minutes

## Ingress & Gateway API

The ingress controller is already installed and running, and two backends (`app1-svc`, `app2-svc`) are already deployed. You write the routing rules only.

**You will:**
1. Route `/app1` to `app1-svc`
2. Add `/app2` to the same Ingress, routed to `app2-svc`

**Target time:** 55 minutes

## Labels & Selectors

Labels are how Services, Deployments, and NetworkPolicies find Pods -- not by name, by label match. This scenario builds three small Pods, then puts selectors to work.

**You will:**
1. Create three Pods with specific label combinations
2. Query them with an AND selector and a set-based selector

**Target time:** 30 minutes

## Network Policies

`frontend` should be able to reach `backend-svc` -- there's already a `NetworkPolicy` (`backend-allow-frontend`) that looks like it permits exactly this. It doesn't work. Find out why, then fix it.

**You will:**
1. Confirm the traffic is blocked, and diagnose the policy bug
2. Fix the policy and confirm traffic flows

**Target time:** 50 minutes
